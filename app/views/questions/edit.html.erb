<h1><%= question_header(@question, :edit) %></h1>

<%= form_with model: [@test, @question], local: true do |form| %>
  <p>
    <%= form.label :body %>
    <%= form.text_field :body %>
  </p>

  <p><%= form.submit %></p>
<% end %>
