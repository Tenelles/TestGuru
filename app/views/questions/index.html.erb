<h3> Questions of "<%= @test.title %> <%= @test.level %>" test</h3>

<p><%= link_to 'New question', new_test_question_path(@test) %></p>

<table>
  <tr>
    <th>Body</th>
    <th colspan="3">Actions</th>
  </tr>

  <%= render @questions %>
</table>
